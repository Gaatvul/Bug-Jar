spring:
    profiles:
    datasource:
        url: jdbc:mysql://localhost:3306/bug-tracker?useSSL=false
        username: root
        password: root
        driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    web: debug

debug: true
---